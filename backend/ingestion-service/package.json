{
  "name": "hyperview-ingestion-service",
  "version": "2.0.0",
  "description": "Hyperview backend ingestion — single HL WS connection + Redis cache + REST API",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.21.1",
    "ioredis": "^5.4.1",
    "ws": "^8.18.0"
  }
}
